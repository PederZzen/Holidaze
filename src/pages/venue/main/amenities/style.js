import styled from "styled-components";

export const Wrapper = styled.div `
    margin: 1rem 0;

    >div {
        margin-top: 1rem;
        display: flex;
        gap: .5rem;
    }

    h2 {
        font-size: 1.2rem;
    }
`